<!DOCTYPE html>
<html lang="<%= typeof locale !== 'undefined' && locale === 'ar' ? 'ar' : 'en' %>" dir="<%= typeof locale !== 'undefined' && locale === 'ar' ? 'rtl' : 'ltr' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'SOLARA - Modern Minimal Luxury' %></title>
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <% if (typeof locale !== 'undefined' && locale === 'ar') { %>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700&family=Tajawal:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <% } else { %>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Urbanist:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <% } %>
    
    <!-- Stylesheets -->
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/forms.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <% if (typeof locale !== 'undefined' && locale === 'ar') { %>
    <style>
        /* RTL Support for Arabic */
        body {
            direction: rtl;
            text-align: right;
            font-family: 'Cairo', 'Tajawal', sans-serif;
        }
        .navbar, .nav-menu, .nav-actions, .user-actions {
            direction: ltr; /* Keep navigation LTR for consistency */
        }
        .language-selector {
            direction: ltr;
        }
    </style>
    <% } %>
    
    <!-- Scroll behavior -->
    <script>
        if (history.scrollRestoration) {
            history.scrollRestoration = 'manual';
        }
        window.onload = function() {
            window.scrollTo(0, 0);
        };
    </script>
</head>
<body>

